print打印输出，input输入
range范围
float浮点数，int整数，str字符串
list列表，dict字典，tuple元组，set集合
star开始，stop结束，step步长
index索引
sort排序，reverse颠倒
append追加，extend延伸，insert插入
remove去除，pop指定位置去除
add添加，update更新，discard丢弃
intersection交集&；union并集|
different差集-；symmetric_difference对称差集^
clear清除，del删除
upper大写，lower小写，swapcase大小写转换
capitalize首大写，余小写
title每个字符首大写，余小写；
center居中对齐，ljust左对齐，rjust右对齐
zfill右对齐，用0填充；
split劈分，用sep指定劈分符
maxsplit最大劈分次数
rsplit从右开始劈分；
isidentifier合法标识符（字母数字下划线）
isspace空白字符（回车换行水平制表符）
isalpha字母字符（字母，汉字为字母）
isdecimal十进制数字（阿拉伯数字，非小数）
isnumeric数字（汉字数、罗马数也算）
isalnum字母数字；
replace替换（old，new，次数）
join合并列表元组的元素为一个元素
encode编码，decode解码
global全局变量
class类


#注释，'''多行注释


运算符：
+加，-减，*乘，/除（+也作连接符）
//整除  9//-4＝-3（向下取整，-2.25→-3）
%取余  11%2＝1  （余数＝被除数-除数*商，商向下取整，9%-4＝-3，-9%4=3）
**幂 11**2＝121

=赋值

比较：
<小于，>大于，<=小于等于，>=大于等于
!=不等于，==等于（value）
（比较字符串时，从第一个字符开始依次比较，ord调取原始值，chr调取字符）

is（id）

布尔：
true是，false非
and与，or或，not非，in在，not in不在

二进制：
&按位与，|按位或
<<左移位，>>右移位



